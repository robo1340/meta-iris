
#include <stddef.h>
#include "header.h"
#include <czmq.h>

frame_header_t header;
uint8_t coded[ENC_HEADER_LEN_BYTES];

/*
struct __attribute__((__packed__)) frame_header_DEFINITION {
	uint8_t callsign[8]; ///<the src callsign of the frame
	uint16_t len; ///<len of the payload that is used
	uint16_t opt;
	uint32_t crc; ///<crc32 of the remaining payload
	uint8_t mac_tail[3]; ///<the last three bytes of the sending radio's MAC	
	uint8_t hdr_crc; ///<crc byte of the header
};
*/

//static uint8_t crc8x_simple(uint8_t crc, void * mem, size_t len);
//static uint8_t crc8x_fast(uint8_t crc, void * mem, size_t len);
static uint8_t crc8x_even_faster(uint8_t crc, void * mem, size_t len);


frame_header_t * decode_frame_header(conv_encoder_t * encoder, uint8_t * to_decode, uint32_t len){
	frame_header_t * hdr_ptr = (frame_header_t*)hdr_conv_encoder_decode(encoder, to_decode, len);
	if (hdr_ptr == NULL){
		return NULL;
	}
	memcpy(&header, hdr_ptr, sizeof(frame_header_t));
	
	uint8_t crc = crc8x_even_faster(0xff, &header, sizeof(frame_header_t)-1);
	if (crc != header.hdr_crc){
		printf("WARNING: hdr CRC mismatch expected 0x%02x, received 0x%02x\n", crc, header.hdr_crc);
		return NULL;
	}
	return &header;
	
}

uint8_t * encode_frame_header(conv_encoder_t * encoder, frame_header_t * to_encode, uint32_t * encoded_len){
	to_encode->hdr_crc = crc8x_even_faster(0xff, to_encode, sizeof(frame_header_t)-1);
	
	uint8_t * coded_ptr = hdr_conv_encoder_encode(encoder, (uint8_t*)to_encode, sizeof(frame_header_t));
	memcpy(coded, coded_ptr, ENC_HEADER_LEN_BYTES);
	*encoded_len = encoder->coded_bytes_len*encoder->conv_blocks;
	return coded; //48 bytes long
}

/*
static uint8_t crc8x_simple(uint8_t crc, void * mem, size_t len) {
    uint8_t const *data = mem;
    if (data == NULL)
        return 0xff;
    while (len--) {
        crc ^= *data++;
        for (uint8_t k = 0; k < 8; k++)
            crc = crc & 0x80 ? (crc << 1) ^ 0x31 : crc << 1;
    }
    crc &= 0xff;
    return crc;
}
*/

static uint8_t const crc8x_table[] = {
    };

/*
static uint8_t crc8x_fast(uint8_t crc, void * mem, size_t len) {
    uint8_t * data = mem;
    if (data == NULL)
        return 0xff;
    crc &= 0xff;
    while (len--)
        crc = crc8x_table[crc ^ *data++];
    return crc;
}
*/

static uint8_t crc8x_even_faster(uint8_t crc, void * mem, size_t len) {
    const uint32_t *data32 = (const uint32_t *)mem;
    const uint8_t *data8;
    if (data32 == NULL) {
        return 0xff;
    }
    crc &= 0xff;
    while (len >= 4) {
        const uint32_t word = *data32++;
        crc = crc8x_table[crc ^ (word & 0xff)];
        crc = crc8x_table[crc ^ ((word >> 8) & 0xff)];
        crc = crc8x_table[crc ^ ((word >> 16) & 0xff)];
        crc = crc8x_table[crc ^ ((word >> 24) & 0xff)];
        len -= 4;
    }
    data8 = (const uint8_t *)data32;
    while (len--) {
        crc = crc8x_table[crc ^ *data8++];
    }
    return crc;
}

